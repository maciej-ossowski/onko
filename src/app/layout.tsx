import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Onko Platform - Cyfrowa platforma dla pacjentek z rakiem piersi",
  description: "Platforma prowadząca pacjentki przez całą ścieżkę leczenia raka piersi. Autoryzacja przez Profil Zaufany, karta DiLO, kalkulator ryzyka AI, monitoring ran i teleopieka.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pl">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
